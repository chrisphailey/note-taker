// read db.json file with FS to get notes
    // create a class, 
// write to db.json file to add new notes

// delete notes from the db.json to file



class DBInteractions {
    read(){
        const response = 'this would be the data returned'
        return response;
    }
}

module.exports = new DBInteractions();